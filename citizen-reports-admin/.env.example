# Producción (o sin proxy): URL completa del backend
VITE_API_BASE_URL=https://backendreporteciudadano-production.up.railway.app/api

# Desarrollo local con proxy (evita CORS): descomenta la siguiente línea y comenta la de arriba
# VITE_API_BASE_URL=/api
